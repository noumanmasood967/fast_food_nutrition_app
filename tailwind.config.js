/** @type {import('tailwindcss').Config} */
export default {
 content: [
    // This tells Tailwind to scan all HTML and JS files inside the 'frontend' folder
    "./frontend/**/*.{html,js}", 
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

