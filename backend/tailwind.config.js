/** @type {import('tailwindcss').Config} */
module.exports = {
  // 🎯 IMPORTANT: Tell Tailwind where to scan for class names
  content: [
    "../frontend/**/*.{html,js}", // Scans all HTML and JS files in the frontend folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}